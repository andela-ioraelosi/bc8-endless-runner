var myApp = angular.module("myApp", ["firebase"]);

if(game.end === true){
	$scope.
}

$scope.authObj.$onAuth(function(authData) {
  if (authData) {
    console.log(authData.uid +" "+  authData.uid.child(score));
  } else {
    console.log("Logged out");
  }
});